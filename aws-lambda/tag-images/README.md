# GhostRoll Post-Upload AI Tagging (AWS Rekognition)

This Lambda watches for uploads under `sessions/*/share/*.jpg` and writes tag JSON sidecars under `sessions/*/tags/*.json`.

See `../README.md` and `../DEPLOYMENT.md` for deployment instructions.

